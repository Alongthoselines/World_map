To make this notebook work, geopandas was installed in conda env. 
Conda env can be used in jupyter-notebook kernel by running following command from the activated env:

python -m ipykernel install --user --name myenv --display-name "Python (myenv)"

Then, jupyter-notebook can be called.


Data can be obtained from site: https://thematicmapping.org/downloads/world_borders.php and unzipped content is assumed to be in ./data directory. 
